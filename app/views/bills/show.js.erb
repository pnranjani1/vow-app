/* 
  In show action we are calling the esnget(bill) function to get the calculated
  result, after the result is fetched, we need to unblock the UI which will be 
  done by below piece of code 

*/

App.unBlockUI($('.portlet-content'));
